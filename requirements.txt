# BFSI Agents Fraud Lab — required runtime (demo / lab)
streamlit>=1.28.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# Optional: Postgres lab backend (DB_BACKEND=postgres)
psycopg2-binary>=2.9.9

# Optional: MCP sample server (python -m fraud_agents.mcp_server)
mcp>=1.0.0

# Dev / test
pytest>=8.0.0
